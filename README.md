# AniTrack
In the front end Client file download dependencies and then npm start

In the backend Server file download dependecies and then npm run dev

note- loading anime's may take a while due to API rate limits
